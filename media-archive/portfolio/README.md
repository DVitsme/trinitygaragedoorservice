# Portfolio photos (captured from the old site)

**Source:** the old WordPress site's `/portfolio/` gallery, `trinitygaragedoorservice.com/wp-content/uploads/2024/09/...`
**Captured:** 2026-06-19 (before WordPress is retired).
**Count:** 30 unique photos, 21 MB — the full-resolution version of each (bare original where
the page referenced it, otherwise the WordPress `-scaled` master).

These are **real completed-job photos** of Trinity's work (the genuine trust asset the audit
called out). They are the "AFTER"/finished-door shots that can pair with the homepage's
before/after section and fill `/about/portfolio/`.

## Caveats (carry into the rebuild)
- **No alt text and non-descriptive filenames** (`Group-33.jpg`, UUIDs) — finding F10 in the
  audit. Add descriptive `alt` + rename before use.
- **No paired "before" photos exist** — these are finished results only. Don't fabricate a
  "before"; pair only where the client supplies the matching before shot.
- Responsive-size duplicates and one exact re-upload (`Group-46-1.jpg`) were removed; two PNGs
  that were actually brand logos (`Image-12.png`, `layer1.png`) were excluded.
- A few are lower-res (the 4 UUID `.jpeg` files); swap for hi-res if the client has originals.

Original placement/role for each image is in `site-audit/01-homepage-and-company.md`
(Portfolio page image table).
